# Weekly-Supervised-Segmentation-
Built a custom PartialFocalLoss in PyTorch that restricts gradient updates to labeled point locations only, integrated with a UNet + ResNet-34 on the LoveDA Urban dataset (7 classes). Two ablation experiments explore how supervision density and focal gamma affect segmentation performance.
